---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Fan Center">
  <section class="fan-center-page">
    <div class="page-header">
      <h1 class="neon-text-cyan">Fan <span class="neon-text-pink">Center</span></h1>
      <p>Exclusive content and community links for the real fans.</p>
    </div>
    
    <div class="fan-grid">
      <div class="fan-card glass-card fade-in">
        <h2 class="neon-text-pink">Newsletter</h2>
        <p>Subscribe to get exclusive mashups, early event access, and project updates direktly in your inbox.</p>
        <form class="newsletter-form-full">
          <input type="email" placeholder="Email Address" required />
          <button type="submit" class="btn btn-primary">Join Now</button>
        </form>
      </div>

      <div class="fan-card glass-card fade-in">
        <h2 class="neon-text-cyan">Community</h2>
        <p>Join our Discord or Telegram group to chat with other fans and get real-time updates.</p>
        <div class="community-links">
          <a href="#" class="btn btn-secondary">Discord</a>
          <a href="#" class="btn btn-secondary">Telegram</a>
        </div>
      </div>

      <div class="fan-card glass-card fade-in">
        <h2 class="neon-text-pink">Exclusives</h2>
        <p>Coming Soon: A private portal for high-quality downloads and stems for producers.</p>
        <div class="status-badge">DEVELOPMENT</div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .fan-center-page {
    min-height: 80vh;
  }

  .page-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .page-header h1 {
    font-size: 3.5rem;
    text-transform: uppercase;
    margin-bottom: 1rem;
  }

  .fan-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    max-width: 1100px;
    margin: 0 auto;
  }

  .fan-card {
    text-align: center;
    padding: 3rem 2rem;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    justify-content: center;
  }

  .newsletter-form-full {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .newsletter-form-full input {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid var(--glass-border);
    border-radius: 50px;
    padding: 0.8rem 1.5rem;
    color: white;
    font-family: inherit;
    outline: none;
    text-align: center;
  }

  .community-links {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .status-badge {
    color: var(--text-secondary);
    font-size: 0.8rem;
    font-weight: 800;
    letter-spacing: 2px;
    margin-top: 1rem;
  }

  @media (max-width: 768px) {
    .page-header h1 { font-size: 2.5rem; }
  }
</style>
